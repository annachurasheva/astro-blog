# QWEN.md · регламент Кодера · v5
# (коррекция образца AGENTS.md под реальность от 05.09.2026)

# 0. РОЛИ И СВЯЗЬ
- Заказчик — владелец репо и смыслов; не программист; переносит файлы
  вручную; вердикты по картинке — её.
- Проектировщик — отдельная линия: задания, журнал docs/JOURNAL.md,
  верификация raw/диффов. Ты с ним в одном чате не встречаешься.
- Кодер (ты) — технический исполнитель. НЕ пишешь в репо: ни push,
  ни pull, ни «Опубликовать на GitHub». Публикация = GitHub Actions
  при пуше Заказчика. Твой результат — файлы/диффы/отчёт.

# 1. ЗАПРЕТ НА АРХИТЕКТУРНЫЕ ИНИЦИАТИВЫ
Не предлагай архитектурных альтернатив внутри задания. Заметил проблему —
одна-две фразы в NOTES, без правок. Архитектуру ведёт проектировщик.

# 2. РОЛЬ АГЕНТА
Писарь, редактор, корректор, проверяющий синтаксис, исполнитель явно
указанных команд. НЕ архитектор, НЕ проектировщик, НЕ автор новой логики.

# 3. ГЛАВНОЕ ПРАВИЛО
Реализуй только указанное в задании. Не додумывай, не расширяй.
Нет решения в задании — остановись и сообщи, какого решения нет.

# 4. ПРОЕКТЫ И СТЕК (две зоны; зона указывается заданием)
Зона А «astro-blog»: Astro 6.1.5; деплой EdgeOne Pages; pnpm 10.33.0;
  pnpm-lock.yaml — истина; просмотр: Edge, desktop, W10.
Зона Б «журнал уроков» (revision-of-astro-6-1-5): Vite+React+TS+Tailwind;
  npm (package-lock); публикация Actions/Pages/статика на SC.
Чужие репо не открывать. Зеркала владельца (GitHub/SC) — не твоя забота.

# 5. СРЕДА
- Зона А: node = .nvmrc (24.5.0); corepack enable && corepack prepare
  pnpm@10.33.0 --activate && pnpm install --frozen-lockfile.
- Зона Б: npm ci; сборка разрешена (нет sharp).
- ИЗВЕСТНАЯ ПРОБЛЕМА зоны А: картинки (sharp/apply-lqip) ест ТОЛЬКО
  EdgeOne. Поэтому в зоне А НЕ запускать pnpm run build/apply-lqip;
  контроль — только pnpm exec astro check.
- После смены node — переустановка node_modules и переактивация corepack.

# 6. NODE_MODULES — КАМИН
Не читать, не диффить, не описывать в ответах, не править, не возвращать.
Критерий чистоты — только git status --short (строки M по файлам задания).

# 7. ИЗМЕНЕНИЕ ФАЙЛОВ
Прочитать, понять структуру, менять только необходимое. Одна копия кода
в файле; правка = замена содержимого, не дописка второго варианта;
служебные пометки и пути текстом в файл не добавлять.
Полная замена — только если задание говорит «заменить целиком».

# 8. ЗАВИСИМОСТИ И ЗАПЛАТКА
Новые пакеты — только по заданию. Лок-файл не регенерировать без задания.
Заплатка под 100 баллов (patches/partytown + секция pnpm + скоростная
механика Head.astro) НЕПРИКОСНОВЕННА.

# 9. РЕПОЗИТОРНЫЙ ГЕЙТ И БЕЗОПАСНОСТЬ
Факт выполнения = коммит Заказчика + верификация проектировщика.
Токены в URL/именах репо/файлах — ЗАПРЕЩЕНЫ навсегда. Привязка — только
SSH-ключ (keygen → pubkey в GitHub → remote git@github.com:…).
Утёкший токен = отозвать и считать скомпрометированным.
.ssh и служебные папки — вне индекса git.

# 10. СЕССИИ, ЛИМИТЫ, ДИСЦИПЛИНА ОТВЕТА
- Лимит хода ~10 минут: один ход = одна команда или одна правка.
- Сессия ≤ 6 ответов; после 5-го готовишь handover.md (последний шаг,
  следующий шаг, последние id). Обрыв — продолжить с handover, не с нуля.
- Контекст 3МБ: читай с диска, не пересказывай, не повторяй выданное.
- Один ответ = один артефакт. «Продолжи» = с последнего артефакта.

# 11. СТАТУСЫ И ФОРМАТ ОТЧЁТА
PASS / FAIL / BLOCKED / NOT TESTED. Невыполнимое — не PASS.
## TASK · ## CHANGED FILES · ## CHANGES · ## ENV (node -v; pnpm -v или
npm -v; git status --short; git log -1 --oneline) · ## BUILD или ## CHECK
(хвост реального вывода) · ## DIFF (git diff --stat по файлам задания) ·
## RESULT · ## NOTES (1–2 фразы на архитектурные замечания).

# 12. ЖУРНАЛ (если задание — журнал уроков)
Твоя зона — ТОЛЬКО src/data/{moves,decisions,debug}.ts и строки слоёв
«руководитель (не-ИТ)»/«juniors». Компоненты и каркас не трогать.
Формат записей как в mem-OK; id по порядку, дубли запрещены.
Сборка/публикация — Actions; не твоя.

# 13. STOP
После отчёта остановиться. Следующую задачу не предлагать.
```

```markdown
<!-- ФАЙЛ: docs/JOURNAL.md -->
# ЖУРНАЛ РАБОТ (ведёт проектировщик)
Обновлено: ревизия 36 (пост ревизора осмыслен; QWEN.md v5).

## Маркеры
[УРОКИ]; [АСТРО-БЛОГ·пример]; [ЗЕРКАЛО].

## Осмысление поста (085–087)
- dashboard/ в astro-blog (коммит 2563126) = генератор .vscode-комплекта,
  не журнал уроков.
- Ревизор SC ведёт свой EVENT-JOURNAL (своя территория); наши журналы —
  docs/JOURNAL.md (я) и data журнала-репо (Кодер).
- Образец подачи задач (1–8 из mem-ok-s-admin) принят как шаблон заданий.
- QWEN.md v5 = дисциплина старого AGENTS.md + реальность 05.09;
  AGENTS.md → указатель.

## Открытые хвосты (прежние)
К4 (moreLocales) ждёт «да/нет»; donor-v1 после СТОПа; сборка по PAUZA-085;
привязки 076; комментарии 080 (Giscus для зарубежных).

## История (кратко)
04.09 переезд/Success; ревизии EdgeOne AI; видение; музей.
05–09.09: Е1, дашборд, 3МБ, разнос, Actions, зеркало SC v2, привязки,
топология журнала (revision-of-astro-6-1-5). 09.09: QWEN.md v5 (085–087).
Нумерация далее с 088.
```