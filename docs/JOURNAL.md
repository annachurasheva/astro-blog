<!-- ФАЙЛ: docs/JOURNAL.md -->
# ЖУРНАЛ РАБОТ (ведёт проектировщик)
Обновлено: цикл «чистый старт», ревизия 2 (после картины про автора/EdgeOne).

## Постоянные факты
- Стенд: W10 кастомная, PowerShell 7.6.5, Edge; `C:\astro-blog\`;
  Node 24.5.0 (.nvmrc); pnpm 10.33.0 (corepack); `pnpm-lock.yaml` — истина;
  порт 4321; буфер обмена ненадёжен — только файлы.
- Кодер: новое окно = без памяти; `git pull` запрещён платформой;
  вход в работу — `AGENTS.md` + `docs/EXEC-LIST.md`.
- `.devcontainer/devcontainer.json` — авторский рецепт точной среды;
  платформы с контейнерами открывают проект через него; нам — просто держать.
- Автор шаблона = автор Astro-примеров в витрине EdgeOne Pages;
  шаблон несёт «заплатку под 100 баллов» Lighthouse — её механику не трогать.
- Приёмка: raw-файлы на GitHub проверяет проектировщик; вердикт по картинке —
  Заказчик.

## Решения Заказчика (ревизия 2) — сверх этого Кодер не трогает НИЧЕГО
- Чужая аналитика — убрать. Чужой хостинг картинок — убрать.
- Позывные — СТАВИТЬ СЕЙЧАС в доноре: author Anna Churasheva;
  почты anna.churasheva@gmail.com и 79787883649@ya.ru;
  название «Мемориал Корпечь, Крым» (из TASK-0003).
- `site.url` — сейчас тестовый домен EdgeOne текущего деплоя;
  в вилках подставляется одной строкой.
- Комментарии — не прикручивать; модули НЕ удалять.
- Языки — НЕ удалять; русский первый.
- «Слежение» за автором (update-theme) — оставить, ЗАМОРОЗИТЬ:
  не запускать без задачи проектировщика.
- Apiflash — убираем через локальный генератор превью; бесплатный ключ —
  только запасной план.

## План «СТОП и вилка»
- Донор = astro-blog после успешного деплоя → СТОП.
- Вилка 1: memorial-korpech-crimea (из донора как есть, домен подставится).
- Вилка 2: копия донора; меняются только `src/config.ts` и `src/content/`.
- Железное правило донора: проектные данные только в `src/config.ts`
  и `src/content/`; код проектных данных не носит.

## Состояние
- Собирается новое тело репо: чистый шаблон + docs-файлы:
  AGENTS.md (v2.2), docs/ABOUT_USER.md, docs/JOURNAL.md, docs/EXEC-LIST.md,
  docs/elimination-of-deficiencies.md, docs/tasks/TASK-0001..0004 (архив).
- TASK-0005 любых версий — аннулированы.
- Очередь «после размещения текстов»: пункты 1–8, 10–12 разбора косяков.

## История (кратко)
- Кодер без руководителя: «чистка» поверхностная (чужая аналитика и хостинг
  картинок остались), главная задача «разместить тексты» пропущена.
- Отсюда: Кодер работает только по EXEC-LIST, по одному пункту за ответ.