<!-- ФАЙЛ: docs/JOURNAL.md -->
# ЖУРНАЛ РАБОТ (ведёт проектировщик)
Обновлено: ревизия 3 (после двух отвалов по 10 минут и чтения ЗАПИСКИ).

## Постоянные факты
- Стенд: W10 кастомная, PowerShell 7.6.5, Edge; `C:\astro-blog\`;
  Node 24.5.0 (.nvmrc); pnpm 10.33.0 (corepack); `pnpm-lock.yaml` — истина;
  порт 4321; буфер обмена ненадёжен — только файлы.
- Кодер: новое окно = без памяти; `git pull` запрещён; вход — `QWEN.md`
  (родной файл, русский) + `docs/EXEC-LIST.md`.
- ЛИМИТ ПЛАТФОРМЫ: ~10 минут на ход. Один ход = одна команда или правка;
  установка и сборка — отдельные ходы. Отвал = стоп и сообщение мне.
- Артефакты Кодера ≠ файлы: комплекты забирать кнопкой
  «Скачать комплект (.zip)».
- `.devcontainer/devcontainer.json` — авторский рецепт среды; держать.
- Шаблон несёт «заплатку под 100 баллов» Lighthouse — механику не трогать.
- Приёмка: raw-файлы на GitHub проверяет проектировщик; вердикт — Заказчик.
- Правило 0 (из ЗАПИСКИ, принято): пара «дельта+кодер» к ПРОДУ
  не допускается; переносит только человек, пошагово.

## Решения Заказчика (ревизия 2, без изменений)
- Чужая аналитика и чужой хостинг картинок — убрать.
- Позывные донора — ставить сейчас: Anna Churasheva; обе почты;
  название «Мемориал Корпечь, Крым».
- `site.url` — сейчас тестовый домен EdgeOne; в вилках — одной строкой.
- Комментарии — не прикручивать, модули не удалять. Языки — не удалять.
- update-theme — заморожен. Apiflash — через локальный генератор;
  бесплатный ключ — только запас.

## План «СТОП и вилка»
Донор = успешный деплой astro-blog → СТОП.
Вилка 1: memorial-korpech-crimea. Вилка 2: копия донора.
Проектные данные — только в `src/config.ts` и `src/content/`.

## Состояние
- Ветка: QWEN.md v1 и указатель AGENTS.md — на месте (коммит 48ba3dac…);
  EXEC-LIST v3, JOURNAL v3 — этим ответом.
- Первый цикл Кодера идёт микро-ходами Ф1–Ф6 (tasks/ANSWER-10min.md).
- TASK-0005 любых версий — аннулированы; TASK-0001..0004 — архив.
- Очередь «после текстов»: пункты 1–8, 10–12 разбора косяков.

## История (кратко)
- Кодер без руководителя: чистка поверхностная, главная задача пропущена.
- AGENTS.md (общая конвенция) заменён родным QWEN.md на русском.
- Два отвала по 10:01 = лимит хода; список нарезан на микро-ходы (v3).