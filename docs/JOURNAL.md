<!-- ФАЙЛ: docs/JOURNAL.md -->
# ЖУРНАЛ РАБОТ (ведёт проектировщик)
Обновлено: ревизия 9 (комплект — диффами; лок-файл под контролем).

## Постоянные факты
- Стенд: W10, PowerShell 7.6.5, Edge; `C:\astro-blog\`; Node 24.5.0 (.nvmrc);
  pnpm 10.33.0 (corepack); `pnpm-lock.yaml` — истина; порт 4321;
  буфер ненадёжен — файлы/диффы принимает Заказчик своим способом.
- Кодер: без памяти, без `git pull`; вход — `QWEN.md` + `docs/EXEC-LIST.md`;
  лимит хода ~10 минут; один ход = одна команда/правка; документы НЕ менять.
- Перенос — ТОЛЬКО человеком (правило 0). Кнопка «Опубликовать на GitHub»
  запрещена. Комплект отдаётся ДИФФАМИ четырёх согласованных файлов.
- Лок-файл: установка только `--frozen-lockfile`; `repository` в
  package.json — метаданные, лок не трогают; контроль — отсутствие
  `pnpm-lock.yaml` в `git status --short`.
- node_modules = «камин». Картинки ест ТОЛЬКО EdgeOne; у Кодера —
  только `astro check`. Нода у Кодера 24.5.0 тарболом в /usr/local.
- Заплатка под 100 баллов Lighthouse — механику не трогать.
- Приёмка: raw на GitHub — проектировщик; вердикт вживую — Заказчик.

## Решения Заказчика (ревизия 2, без изменений)
Чужая аналитика/хостинг картинок — убрать; позывные донора — ставить сейчас;
`site.url` — тестовый домен до вилок; комментарии и языки — не трогать;
update-theme — заморожен; Apiflash — локальный генератор, бесплатный ключ — запас.

## План «СТОП и вилка»
Донор → СТОП после живой приёмки → вилка 1 memorial-korpech-crimea;
вилка 2 копия донора. Проектные данные — только `src/config.ts` и `src/content/`.

## Состояние
- Кодер прошёл пункты 1–11 EXEC-LIST v5; пункт 11 принят.
- Выдано Д1: диффы 4 файлов + контроль git status (лок и node_modules — вне).
- После правок человеком: моя raw-сверка, живой деплой, приёмка → СТОП.
- TASK-0005* аннулированы; TASK-0001..0004 архив.

## История (кратко)
- Кодер без руководителя: чистка поверхностная, главная задача пропущена.
- AGENTS.md → родной QWEN.md; отвалы 10:01 → микро-ходы.
- apt-нода 20.x → тарбол 24.5.0; build снят → astro check.
- Шум .d.ts.map — «камин»; кнопка публикации — запрещена.
- 04.09: zip заменён диффами; лок-файл подтверждён флагом frozen-lockfile.