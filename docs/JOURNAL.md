<!-- ФАЙЛ: docs/JOURNAL.md -->
# ЖУРНАЛ РАБОТ (ведёт проектировщик)
Обновлено: ревизия 14 (куски диффов отменены; перенос через git apply).

## Постоянные факты
- Стенд: W10, PS 7.6.5, Edge; `C:\astro-blog\` — клон с правом пуша;
  Node 24.5.0; pnpm 10.33.0; лок — истина; порт 4321; буфер ненадёжен.
- Кодер: без памяти/pull; лимит ~10 мин; один ход = одна команда/правка;
  кнопка «Опубликовать» запрещена; node_modules = «камин»; только astro check.
- Ш1 шум игнорируем мы; Ш2 Кодер не выводит его на экран.
- og:image — абсолютный; await в генераторе — под eslint-disable.
- Деплой с живыми шаблонными постами запрещён (clean-posts.ps1 до деплоя).
- Заплатка под 100 баллов Lighthouse — механику не трогать.
- Приёмка: raw — проектировщик; вердикт вживую — Заказчик.
- Нумерация ответов Заказчику сквозная: выдано 002–010; далее с 011.
- ПЕРЕНОС: один накопительный final.patch + `git apply` у Заказчика;
  кусковые диффы запрещены (источник непонятности 04.09).

## Решения по конфигу (ревизия 14)
- Заглушки шаблона GA-XXXXXXXXXX и @yourtwitter — в пустые строки
  (GA-заглушка тянет чужой скрипт статистики).
- package.json: version 1.0.0 (самовольный bump отменён);
  repository = репозиторий Заказчика.
- Подзаголовок «Памяти жертв политических репрессий» принят;
  правка текстов — позже, только через src/config.ts.

## Решения по превью (ревизия 13, без изменений)
Сейчас статику НЕ класть; генератор рисует сам. План Б — public/og/index.png
+ убрать запись index из генератора, если /og/index.png не откроется вживую.
Своя идентичность — заменой public/icons/og-logo.png (имя то же).

## План «СТОП и вилка»
Донор → СТОП после живой приёмки → вилка 1 memorial-korpech-crimea;
вилка 2 копия донора. Проектные данные — только src/config.ts и src/content/.

## Состояние
- Раунд 2 сверен (007/008). Выданы И4, И5 (final.patch).
- После: git apply → Д0–Д3 → push → raw-сверка → деплой → приёмка → СТОП.
- TASK-0005* аннулированы; TASK-0001..0004 архив.

## История (кратко)
- Кодер без руководителя: чистка поверхностная, главная задача пропущена.
- QWEN.md на русском; микро-ходы; тарбол 24.5.0; astro check вместо build.
- Кнопка публикации запрещена; zip→диффы→final.patch+git apply.
- 04.09: И1/И2 приняты; заглушки GA/twitter и version/repository → И4.