<!-- ФАЙЛ: docs/JOURNAL.md -->
# ЖУРНАЛ РАБОТ (ведёт проектировщик)
Обновлено: ревизия 6 (сборка уезжает на EdgeOne, Кодеру — astro check).

## Постоянные факты
- Стенд: W10, PowerShell 7.6.5, Edge; `C:\astro-blog\`; Node 24.5.0 (.nvmrc);
  pnpm 10.33.0 (corepack); `pnpm-lock.yaml` — истина; порт 4321;
  буфер ненадёжен — только файлы.
- Кодер: без памяти, без `git pull`; вход — `QWEN.md` + `docs/EXEC-LIST.md`;
  лимит хода ~10 минут; один ход = одна команда/правка;
  комплекты — кнопкой «Скачать комплект (.zip)»; документы НЕ изменять.
- Пространство Кодера: Linux-контейнер без nvm; node 24.5.0 — тарболом
  в `/usr/local`; сменил ноду — переустанови `node_modules` + corepack.
- ИЗВЕСТНАЯ ПРОБЛЕМА: картинки шаблона (sharp/apply-lqip) ест ТОЛЬКО
  EdgeOne Pages. Кодер НЕ запускает `pnpm run build`/`apply-lqip`;
  контроль — `pnpm exec astro check`. Полная сборка и приёмка — на EdgeOne
  после переноса файлов человеком.
- Правило 0: пара «дельта+кодер» к ПРОДУ не допускается; переносит человек.
- Заплатка под 100 баллов Lighthouse — механику не трогать.
- Приёмка: raw на GitHub — проектировщик; вердикт вживую — Заказчик.

## Решения Заказчика (ревизия 2, без изменений)
Чужая аналитика/хостинг картинок — убрать; позывные донора — ставить сейчас;
`site.url` — тестовый домен до вилок; комментарии и языки — не трогать;
update-theme — заморожен; Apiflash — локальный генератор, бесплатный ключ — запас.

## План «СТОП и вилка»
Донор → СТОП → вилка 1 memorial-korpech-crimea; вилка 2 копия донора.
Проектные данные — только `src/config.ts` и `src/content/`.

## Состояние
- Выданы ходы Н1–Н5; Н6 заменён на `astro check` (EXEC-LIST v5).
- После комплекта: перенос 4 файлов человеком, приёмка вживую
  (сайт, `/og/index.png`, Ctrl+F «apiflash» = 0).
- TASK-0005* аннулированы; TASK-0001..0004 архив.

## История (кратко)
- Кодер без руководителя: чистка поверхностная, главная задача пропущена.
- AGENTS.md → родной QWEN.md на русском.
- Отвалы по 10:01 = лимит хода → микро-ходы.
- apt-нода 20.x → тарбол 24.5.0 в /usr/local.
- 04.09: build у Кодера снят (известная проблема с картинками) → astro check.