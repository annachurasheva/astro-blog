<!-- ФАЙЛ: docs/JOURNAL.md -->
# ЖУРНАЛ РАБОТ (ведёт проектировщик)
Обновлено: ревизия 12 (сверка диффов + две поправки).

## Постоянные факты
- Стенд: W10, PS 7.6.5, Edge; `C:\astro-blog\` — клон с правом пуша;
  Node 24.5.0; pnpm 10.33.0; лок — истина; порт 4321; буфер ненадёжен.
- Кодер: без памяти/pull; новое окно = свежая копия; лимит ~10 мин;
  один ход = одна команда/правка; кнопка «Опубликовать» запрещена;
  комплект — диффами; node_modules = «камин»; у Кодера только astro check.
- og:image — только абсолютный (new URL с origin): соцсети не едят относительный.
- Верхнеуровневый await в og-генераторе — только под
  eslint-disable-комментарием (оба места).
- Деплой с живыми шаблонными постами запрещён (clean-posts.ps1 до деплоя).
- Заплатка под 100 баллов Lighthouse — механику не трогать.
- Приёмка: raw — проектировщик; вердикт вживую — Заказчик.

## Решения Заказчика (ревизия 2, без изменений)
Чужая аналитика/хостинг картинок — убрать; позывные донора — ставить сейчас;
site.url — тестовый домен до вилок; комментарии и языки — не трогать;
update-theme — заморожен; Apiflash — локальный генератор, ключ фри — запас.

## План «СТОП и вилка»
Донор → СТОП после живой приёмки → вилка 1 memorial-korpech-crimea;
вилка 2 копия донора. Проектные данные — только src/config.ts и src/content/.

## Состояние
- Диффы Head.astro и [...image].ts сверены: apiflash удалён, index-запись
  верна; статус = 4×M. Выданы И1 (абсолютный og:image), И2 (eslint-disable),
  И3 (check + полные диффы всех четырёх, включая конфиг и package.json —
  их диффы ещё не сверены).
- После И3: карандаш ×4 → Д0–Д4 → raw-сверка → деплой → приёмка → СТОП.
- TASK-0005* аннулированы; TASK-0001..0004 архив.

## История (кратко)
- Кодер без руководителя: чистка поверхностная, главная задача пропущена.
- AGENTS.md → QWEN.md; отвалы 10:01 → микро-ходы; apt-нода → тарбол 24.5.0.
- Build у Кодера снят (картинки ест EdgeOne) → astro check.
- Zip → диффы; кнопка публикации запрещена; clean-posts.ps1 в цепочке.
- 04.09: сверка диффов; поправки: абсолютный og:image, eslint-disable.