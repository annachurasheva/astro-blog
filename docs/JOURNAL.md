<!-- ФАЙЛ: docs/JOURNAL.md -->
# ЖУРНАЛ РАБОТ (ведёт проектировщик)
Обновлено: ревизия 16, последняя перед паузой (заплатка partytown опознана).

## Постоянные факты
- Стенд: W10, PS 7.6.5, Edge; `C:\astro-blog\` — клон с правом пуша;
  Node 24.5.0; pnpm 10.33.0; лок — истина; порт 4321; буфер ненадёжен.
- Кодер: без памяти/pull; лимит ~10 мин; один ход = одна команда/правка;
  кнопка «Опубликовать» запрещена; node_modules = «камин»; только astro check.
- Ш1 шум игнорируем мы; Ш2 Кодер не выводит его на экран.
- og:image — абсолютный; await в генераторе — под eslint-disable.
- Деплой с живыми шаблонными постами запрещён (clean-posts.ps1 до деплоя).
- Приёмка: raw — проектировщик; вердикт вживую — Заказчик.
- Нумерация: выдано 002–015; далее с 016.
- Перенос: один final.patch + git apply; куски запрещены.

## Заплатка под 100 баллов (точное определение, ревизия 16)
- Файл `patches/@qwik.dev__partytown@0.11.2.patch` + секция
  `pnpm.patchedDependencies` в package.json: partytown не проксирует новые
  оконные API (sharedStorage, AttributionReporting…) — нет ошибок в консоли,
  Lighthouse не снимает баллы; плюс фикс имён переменных сниппета.
- Вторая половина «заплатки» — скоростная механика Head.astro
  (preload, отложенные стили). Обе половины НЕ трогать.
- Запреты: папка patches/ неприкосновенна; секция `pnpm` в package.json
  неприкосновенна (И4 = только version + repository).
- При аналитике в '' partytown не рендерится — безвредно, оставляем
  как механику донора/вилок.

## Принцип лояльности EdgeOne (ревизия 15, без изменений)
Картинки — в репо, обработка на сборке, EdgeOne раздаёт свои копии;
внешние хостинги — вон; кодовая механика шаблонная остаётся.

## Решения по конфигу/превью (ревизии 13–14, без изменений)
Заглушки GA/twitter — в ''; позывные и название донора — стоят;
статику превью сейчас не кладём; план Б — public/og/index.png + убрать
запись index из генератора; своя идентичность — заменой og-logo.png.

## План «СТОП и вилка»
Донор → СТОП после живой приёмки → вилка 1 memorial-korpech-crimea;
вилка 2 копия донора. Проектные данные — только src/config.ts и src/content/.

## Состояние (ПАУЗА)
- Репозиторий чистый до OTVET-007-010.md; правки — только у Кодера (4×M).
- package.json в ветке: repository всё ещё чужой — И4 обязателен.
- Точка возврата: И4 (уточнённый, 015) → И5 (final.patch) → процедура 010 →
  raw-сверка → деплой → приёмка → СТОП.
- TASK-0005* аннулированы; TASK-0001..0004 архив.

## История (кратко)
- Кодер без руководителя: чистка поверхностная, главная задача пропущена.
- QWEN.md на русском; микро-ходы; тарбол 24.5.0; astro check вместо build.
- Кнопка публикации запрещена; zip→диффы→final.patch+git apply.
- 04.09: И1/И2 приняты; пауза; заплатка partytown опознана и защищена.