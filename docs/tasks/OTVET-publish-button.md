<!-- ФАЙЛ: tasks/OTVET-publish-button.md -->
# Ответ: кнопка «Опубликовать на GitHub» — НЕ наш маршрут

## Оценка работы Кодера
Пункт 11 сделан верно: запись `index` берёт title/description из
`themeConfig.site`, посты разложены спредом, `astro check` — 0 ошибок,
0 предупреждений. Правило Ш1 процитировал — дисциплина держится.
Претензий к коду нет.

## Что НЕ делать
Кнопка «Опубликовать на GitHub» сверху справа — это попытка платформы
залить в ветку ВСЁ, что слева в списке, включая node_modules.
Это та самая ловушка, с которой началась задача. Правило 0:
к ПРОДУ допускается только человек. Кнопку НЕ нажимать.
(Исключение-запаска: если платформа даст явно отметить галочками
РОВНО 4 файла и ничто иное — пришлите мне скрин списка ДО нажатия,
я разрешу или запретю.)

## Ваши фразы Кодеру (по одной)
К1: Кнопку «Опубликовать на GitHub» не использовать никогда:
    в её набор попадает node_modules. Переносит только человек.
    Выполни пункт 12: ТОЛЬКО pnpm exec astro check — ответь хвостом.

К2: Пункт 13: собери согласованный комплект ТОЛЬКО из четырёх файлов:
    src/config.ts, package.json, src/layouts/Head.astro,
    src/pages/og/[...image].ts — и отдай кнопкой
    «Скачать комплект (.zip)». Ничего сверх.

## После получения zip (ваши движения, по одному за цикл)
1. Распаковать zip в отдельную папку (ПКМ → извлечь все).
2. Загрузить в GitHub через Add file → Upload files, по одному файлу
   в его папку, с коммитом:
   - src/config.ts → папка src/
   - package.json → корень
   - src/layouts/Head.astro → папка src/layouts/
   - src/pages/og/[...image].ts → папка src/pages/og/
   (имя с квадратными скобками загружается как есть).
3. Сообщить мне хэш последнего коммита — сверяю raw-файлы.
4. EdgeOne соберёт сам; приёмка вживую: сайт открывается;
   `/og/index.png` открывается; Ctrl+F «apiflash» в коде страницы — 0;
   вердикт по картинке — ваш. Это и будет ТОЧКА СТОП донора.