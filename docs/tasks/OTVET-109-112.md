<!-- ФАЙЛ: tasks/OTVET-109-112.md -->
# 109–112 · Ключ восстановлен; регламент трёх репо; ветки; QWEN v6

## 109 [ЗЕРКАЛО] · Принято, без вины
icacls сработал — значит, копия ключа в C:\astro-projects\.ssh живая и
права теперь верные; fetch origin по SSH зелёный. Хоровод окончен:
глобальный sshCommand остаётся на C:/astro-projects/.ssh/astro_blog;
ключ больше никуда не копировать и не переносить.

## 110 [ЗЕРКАЛО] · Регламент переключения трёх репо на ключ
В КАЖДОМ из трёх (astro-blog, memorial-korpech-crimea,
revision-of-astro-6-1-5), по очереди:
git remote -v            (origin обязан быть git@github.com:…)
git config --unset core.sshCommand   (снять локальные перебивки, если были)
git fetch origin
git branch -vv
git remote remove origin2   (если origin2 есть; HTTPS больше не нужен)
Вывод трёх git branch -vv — мне. Глобальный sshCommand уже указывает на
ключ; локально ничего больше не настраиваем.

## 111 [ЗЕРКАЛО/УРОКИ] · Ветки: bak + разработка; потом PR
revision-of-astro-6-1-5:
git switch main
git branch bak main
git switch -c qwen-dashboard-coder main
git push -u origin bak qwen-dashboard-coder
memorial-korpech-crimea:
git switch main
git branch bak main
git switch -c qwen-memorial-coder main
git push -u origin bak qwen-memorial-coder
astro-blog: заморожен — ветки не создавать, только проверка из 110.
Правило: main = приёмка/публикация; bak = стартовое состояние, не трогать;
вся работа — в dev-ветках.
PR-наука (после первой принятой работы): GitHub Web → Pull requests →
New pull request → base: main, compare: qwen-dashboard-coder → Create →
Squash and merge. Dev-ветку НЕ удалять — она рабочая постоянно.

## 112 [УРОКИ] · QWEN.md проверен; выдаю v6 + журнал и задачи в репо
В репо лежит v5 с чужеродным вшитым блоком журнала; упоминание
.vscode-комплекта есть (комплекта НЕТ и не будет); журнала действий
и выдачи заданий внутри поля НЕТ. Исправляю:
- блок 2: QWEN.md v6 (заменяет файл в корне revision);
- блок 3: docs/TASKS.md (выдача заданий; первый урок T-01 — SEO на
  примере memorial) и docs/JOURNAL-ACTIONS.md (ваш журнал действий).
- Кодер-приложение: добавить галочку репо memorial-korpech-crimea
  (иначе T-01 не выполнить) — одно движение в Configure.
- Первый урок идёт в memorial ДО включения его в EdgeOne Pages;
  после приёмки T-01 вы включаете мемориал — материал издателям.