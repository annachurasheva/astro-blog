<!-- ФАЙЛ: tasks/PICTURE-and-STRATEGY.md -->
# Картина целиком и мои вердикты

## Что у вас в руках (складываю ваши домыслы в картину)
- Автор сделал не просто шаблон, а шаблон + «заплатка под 100 баллов»:
  в Head.astro специально разложены preload шрифтов, отложенные стили,
  partytown и прочее — это и есть ваш скрин 100/100/100/100.
- Этот же автор положил два Astro-шаблона в витрину EdgeOne Pages
  (TencentEdgeOne/pages-templates). Значит EdgeOne деплоит такие репо
  «как родные» — это плюс, трогаем механику деплоя ноль раз.
- `.devcontainer/devcontainer.json` — рецепт ТОЧНОЙ среды
  (node 24.5.0, pnpm 10.33.0, порт 4321). Он не для вас и не для вашего ПК.
  Его читают платформы, которые открывают проект «в контейнере» —
  например, платформа Кодера. Поэтому у автора «не бывает косяков сборки»:
  среда всегда одна и та же. Вам делать с ним ничего не надо — просто
  держать в репо.

## Ответ на ваш вопрос «оставить слежение, но заменить позывные?»
ДА, и вот точная раскладка:
- «Слежение» (scripts/update-theme.ts, ссылка на автора) — ОСТАВИТЬ,
  но ЗАМОРОЗИТЬ: не запускать без отдельной задачи от меня.
  Это ваш канал будущих исправлений автора, на деплой не влияет.
- Позывные — СТАВИТЬ СЕЙЧАС в доноре (Anna Churasheva, почты из TASK-0003,
  название «Мемориал Корпечь, Крым»). Позывные ≠ домен: домен придёт позже
  и подставится одной строкой, а обоим вилкам позывные нужны сразу.
- «Лояльность EdgeOne» ловится не позывными, а владением аккаунта и репо —
  она УЖЕ ваша. Позывные ставим ради идентичности сайта и обеих вилок.

## Стратегия «СТОП и вилка» (фиксирую)
Донор = успешный деплой astro-blog. После СТОПа:
- вилка 1 = memorial-korpech-crimea (рождается из донора как есть);
- вилка 2 = копия донора, меняются только конфиг и контент.
Отсюда железное правило донора: ВСЁ проектное живёт только в
`src/config.ts` и `src/content/`. Код не носит проектных данных.
Тогда вилка = копия репо + правка одного конфига + свои тексты.

## Что меняется в моих бумагах (версии ниже, заменить при сборке тела)
- AGENTS.md v2.2: среда через devcontainer (если платформа умеет) +
  запрет трогать «заплатку под 100».
- EXEC-LIST v2: пункт 3 теперь включает позывные и package.json;
  пакет передачи = 4 файла.
- JOURNAL v2: решения обновлены, план вилок записан.

## Ваше движение сейчас
Продолжить сборку нового тела репо; файлы брать из ЭТОГО ответа
(версии v2/v2.2 заменяют прежние). Остальное без изменений.