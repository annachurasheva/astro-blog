<!-- ФАЙЛ: tasks/ANSWER-10min.md -->
# Ответ про два отвала по 10 минут

## Диагноз
Да, версия верная, но уточняю: неподъёмен не смысл приказа, а его ОБЪЁМ
в один ход. Оба отвала случились на 10:01 — это лимит длительности одного
хода платформы. Мой пункт 1 паковал в один ход: чтение двух файлов +
corepack + ТЯЖЁЛУЮ установку (sharp, canvaskit-wasm, mermaid, playwright).
Это больше десяти минут — платформа рвёт связь.
Ждать бесполезно: тот же ход упадёт так же. Лечит только нарезка на микро-ходы.

## Что беру из ЗАПИСКИ (без эмоций, только инженерное)
1. Артефакты ≠ файлы: у Кодера комплекты забирать кнопкой
   «Скачать комплект (.zip)», не обычной Download.
2. Кодер отвечает хэшами/короткими списками, контекст читает с диска,
   не пересказывает — наши файлы-на-диске (QWEN.md, EXEC-LIST, JOURNAL)
   уже так и устроены.
3. Правило 0 принимаю как своё: пара «дельта+кодер» к ПРОДУ не допускается;
   в нашу схему это уже вшито (Кодер не пушит, переносит только человек).
4. Отчёты — источник истины: у нас это docs/JOURNAL.md.

## Новое правило платформы (во все бумаги)
Один ход Кодера = одна команда или одна правка.
Установка и сборка — ВСЕГДА отдельные ходы.
Отвал = стоп, не повторять то же, сообщить мне.

## Ваша последовательность фраз (подавать ПО ОДНОЙ, по порядку)
Если ответ нормальный — следующую. Если отвал — стоп и написать мне.
В первую фразу вставьте вашу ссылку на коммит, как уже делали.

Ф1: Прочитай QWEN.md. Ответь одной строкой: READ. тут <ссылка на коммит>
Ф2: Прочитай docs/EXEC-LIST.md. Ответь одной строкой: LIST.
Ф3: Выполни ТОЛЬКО: corepack enable && corepack prepare pnpm@10.33.0 --activate
    Ответь хвостом вывода.
Ф4: Выполни ТОЛЬКО: pnpm install --frozen-lockfile — ответь хвостом вывода.
Ф5: Выполни ТОЛЬКО: node -v; pnpm -v; git status --short; git log -1 --oneline
    — ответь выводом.
Ф6: Выполни ТОЛЬКО: pnpm run build — ответь хвостом вывода.

После Ф6 принесите мне все шесть ответов — принимаю базовую линию
и выдаю следующие микро-фразы (правки конфига и Apiflash).

## Бумаги обновлены
- docs/EXEC-LIST.md — v3 (микро-ходы, блок 2).
- docs/JOURNAL.md — v3 (факты про лимит 10 минут, zip-кнопку, ЗАПИСКУ).