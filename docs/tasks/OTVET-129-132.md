```markdown
# OTVET-129-132 — отчёт Кодера принят с замечанием; T-02 коррекция; VSCode; координаты

Координаты: `annachurasheva/revision-of-astro-6-1-5` · `docs/tasks/OTVET-129-132.md` · 2026-09-07
Уровень читателя: начальный. Всё — простыми словами.
Сила документов: этот > всё остальное.

## 129 · Отчёт Кодера (report-revision-1.md, c9d0a39) — принят с замечанием
Хорошо: он ВЕРНО различил два проекта (дашборд Vite ≠ Astro-мемориал)
и не стал ломать чужое. Это и есть зрелое поведение по QWEN.
Плохо: файл не отдан маркированным блоком — вы получили его копипастой,
а не переносом готового блока. Замечание и директива (подать Кодеру):
«Замечание: отчёт принят, но файл не отдан как маркированный блок.
Директива (входит в QWEN v6.1): каждый файл в ответе — отдельный
код-блок, начинающийся со строки „# ФАЙЛ: <путь>“. Блок без маркировки —
не файл. Владелец переносит только маркированные блоки. Ответ без
блока для нового файла = задание не выполнено.»

## 130 · Коррекция T-02 и нулевой раздел TASKS (его путаница «двух проектов»)
Добавить в docs/TASKS.md нулевой раздел (и тогда T-02 отправлять можно):
«# 0. ПОЛЯ И РЕПО
Поле уроков: revision-of-astro-6-1-5 (этот репо, дашборд) — задачи T-02+.
Поле мемориала: memorial-korpech-crimea (соседний репо, Astro) — задачи T-01+.
Каждая задача указывает РЕПО. Если репо задачи ≠ текущий репо —
не исполнять; сообщить: „задача для другого поля“.»
T-02 (шапка «Пять шагов») — без изменений, из OTVET-128.
T-01 v2 уходит в ОКНО memorial (после галочки репо в приложении Кодера).

## 131 · Лечение VSCode (простое, по пунктам)
1. Терминал в окне старый (показывает main-qwen3-coder-plus-v01 и origin2):
   закрыть его (корзина), открыть новый: Ctrl+` — ветка станет текущая.
2. Ctrl+Shift+P → «Reload Window» — индикатор ветки внизу и SCM
   перечитаются (origin2 удалён, призрак уйдёт).
3. Если внизу слева ветка чужая — клик по ней → выбрать
   qwen-dashboard-coder.
4. Два окна — нормально, но держите правило: внешнее окно astro-blog =
   переписка/архив; внутреннее revision = работа. Не смешивать терминалы.
5. Порядок главной ветки: origin/HEAD у вас смотрит на qwen-dashboard-coder.
   Верните default на GitHub: Settings → Branches → default branch = main;
   затем локально: git remote set-head origin main.

## 132 · Координаты и хэши
- Координата OTVET-126-128 (revision/docs/tasks) ВЕРНА для рабочего поля;
  копия в astro-blog/docs/tasks = архив переписки. Два места — не ошибка.
- c9d0a39 (отчёт Кодера в revision, qwen-dashboard-coder) — принят.
- b6ea5fcc в astro-blog по адресу reports/report-revision-1.md не нашёлся
  (404). Не важно: содержание у меня есть; в следующий раз кладите отчёты
  мне под именем reports/report-OTVET-NNN.md — и всё сойдётся.
- INSTRUCTION.md (конструкт 3.5) в репо у Кодера — a5ab680 — принято;
  теперь ссылки на 3.5 в TASKS для него легитимны.
```

```markdown

```