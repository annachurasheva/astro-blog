<!-- ФАЙЛ: tasks/OTVET-076-080.md -->
# 076–080 · Привязки, доступы, очередность, комментарии, ghu_

## 076 [ЗЕРКАЛО] · Нужно ли дополнительно верифицировать меня — НЕТ
Я в этом диалоге работаю без ваших привязок: читаю публичное, пишу вам.
На скринах «Qwen Studio (alibaba-oss, Never used)» — это НЕ я, а отдельный
продукт; не пользуетесь — отзовите (Revoke), поверхность меньше.
«Qwen Coding Agent» — это платформа Кодера: держим, пока Кодер работает.
Гигиена по остальному (никогда не использовано → отозвать):
OpenRouter, Roo Code Cloud, Dev Tunnels, Docker Inc, Waline
(Waline — чужой сервис шаблона, нам не нужен; совпадает с нашей чисткой).
Оставить: Git Credential Manager, GitHub Desktop, Visual Studio Code,
EdgeOne Makers, Qwen Coding Agent. «Revoke all» НЕ нажимать.

## 077 [УРОКИ] · Доступ Кодера: расширить, но только по факту задачи
Сейчас у Qwen Coding Agent один репо и одна ветка — это правильно.
Расширение делаем ОДИН раз, когда создан репо журнала:
Settings → Applications → Installed GitHub Apps → Qwen Coding Agent →
Configure → «Only select repositories» → добавить memorial-korpech-crimea.
Ветки astro-blog НЕ расширяем; лишних прав «про запас» не выдаём.

## 078 [ЗЕРКАЛО] · Про происхождение скрипта изолятора
Нормально, что SCA собрал его по рабочему образцу с обёрткой: происхождение
не важно, важна ревизия. Ревизия сделана (v2, FIX-1..8). Запускаете ТОЛЬКО v2
(блок из прошлого ответа), старый файл перезаписываете. Предпосылка v2 —
наличие C:\Tools\Scripts\ssh.bat и C:\Tools\OpenSSH; раз образец с обёрткой
работал, они есть.

## 079 [ЗЕРКАЛО/УРОКИ] · Верная очередность (по одному движению за цикл)
1. Сохранить git-isolated-sc.ps1 v2 в scripts\ (перезаписать).
2. Запустить raznos-journal.ps1 (фаза А) — каркас журнала лёг в
   memorial-korpech-crimea; лог в reports\raznos-journal.log.
3. [АСТРО-БЛОГ·пример] Правка К2 в src/config.ts: строка футера
   url: '79787883649@ya.ru' (без mailto:). Вердикт К4 тоже сейчас:
   рекомендую moreLocales: [] до переводов — скажите «да/нет».
4. Запустить git-isolated-sc.ps1 v2 БЕЗ ключа; окно остаётся;
   пришлите хвост вывода (проверки SC/GitHub).
5. В том же окне: git add .gitignore src/config.ts; git commit
   -m "chore: untrack .ssh, ignore nested, bare email footer";
   git push origin main-qwen3-coder-plus-v01; git push sc main-qwen3-coder-plus-v01
   (снимок .ssh уже в индексе после FIX-4 — войдёт в этот коммит).
6. GitHub Web: создать ПУСТОЙ репо memorial-korpech-crimea.
7. raznos-journal.ps1 ещё раз → фаза Б (ответ «да») → пуш журнала.
8. GitHub: memorial-korpech-crimea → Settings → Pages → Source: GitHub Actions.
9. Зеркало астро-блога на SC: в изолированном окне
   git push sc main main-qwen3-coder-plus-v01; git push sc --tags
   (или перезапуск с -PushMirror).
10. Расширить Qwen Coding Agent (077); новое окно Кодера на
   memorial-korpech-crimea; итерация I-1.
VSCode-профиль с annachurasheva не конфликтует: скрипты идут в pwsh,
вне профилей.

## 080 [УРОКИ] · Комментарии для «зарубежного электората» + правило ghu_
Факт принят: EdgeOne (3200+ CDN) виден из-за рубежа, ОК/ВК там bloccаны;
значит комментарии для зарубежных родственников живут НА EdgeOne, вне
РФ-сервисов. Решение: зарубежный контур — Giscus (дискуссии GitHub:
из Германии GitHub доступен; компонент в шаблоне уже есть, поля пустые —
заполним в фазе 4). Внутренний контур (РФ) — ОК/ВК позже, как и планировали.
ghu_: из уроков убираем историю с токеном, но ЖЁСТКО вставляем правило
привязки (текст ниже — в данные урока, этап 1):

### КАК ПРИВЯЗЫВАТЬ (правило, без исключений)
1. Никогда не вставлять токен в имя репо, в URL remote или в файлы репо.
2. Привязка — только SSH-ключ: ssh-keygen -t ed25519 → публичный ключ в
   GitHub (Settings → SSH and GPG keys) → remote git@github.com:owner/repo.git.
3. Токен допустим ТОЛЬКО внутри credential-менеджера, никогда в URL.
4. Утёкший токен = отозвать немедленно; считать скомпрометированным навсегда.