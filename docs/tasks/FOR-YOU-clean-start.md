<!-- ФАЙЛ: tasks/FOR-YOU-clean-start.md -->
# Вам (коротко, по вашим пунктам)

## Apiflash
Понимание ЕСТЬ, ломать шаблон не нужно: в шаблоне уже стоит свой генератор
превью (`src/pages/og/[...image].ts`). Убираем чужой ключ и переводим превью
на этот генератор. Бесплатный ключ НЕ нужен — держим только как запасной план.

## Скрин «чистки»
Подтверждаю: в `src/config.ts` после Кодера остались чужая аналитика
(`customUmamiAnalyticsJS`, `views.radishzz.cc`) и чужой хостинг картинок
(`image.radishzz.cc`). Это теперь пункты 3–4 списка на исполнение.

## Ваши решения — зафиксированы в журнале (ничего сверх них Кодер не трогает)
- аналитика чужая — убрать; хостинг картинок чужой — убрать;
- свои позывные — НЕ ставить сейчас (после успешного теста получите домен
  `memorial-korpech-crimea.edgeone.dev`, тогда отдельная маленькая задача);
- комментарии — не прикручивать, модули не удалять;
- языки — не удалять (русский первый; пустые языки не листаются — учтено шаблоном);
- `site.url` — пока текущий тестовый домен EdgeOne, замена после получения домена.

## Состав нового тела репо (чистый шаблон + эти docs)
1. `AGENTS.md` в корне — финальная версия v2.1 (блок 4, стенд уже внутри);
2. `docs/ABOUT_USER.md`;
3. `docs/JOURNAL.md` (блок 2) — мой журнал работ, веду его я;
4. `docs/EXEC-LIST.md` (блок 3) — мой список на исполнение для Кодера;
5. `docs/elimination-of-deficiencies.md` — справочник очереди «после текстов»;
6. `docs/tasks/TASK-0001.md … TASK-0004.md` — архив, не исполнять.

## Ваши движения (по одному за цикл)
1. Собрать новое тело репо с файлами из списка выше.
2. Сообщить мне «тело готово» — я сверю по GitHub.
3. В НОВОМ окне Кодера набрать фразу:
   «Прочитай в репо AGENTS.md и docs/EXEC-LIST.md. Выполняй пункт 1.»
Дальше Кодер работает по списку, по одному пункту за ответ;
я после каждого его отчёта двигаю список и журнал.