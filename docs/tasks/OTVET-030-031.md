<!-- ФАЙЛ: tasks/OTVET-030-031.md -->
# 030–031 · Всё на месте. Добро на перезапись main

## 030 · Принято по коммитам
- a27f095: строка 37 Head.astro на скрине — ровно та, что нужна:
  `new URL(..., Astro.url.origin)`. og:image теперь абсолютный. Принято.
- be73cf8: .gitattributes прочитал — состав правильный: текст в LF,
  .ps1 в CRLF (под ваш стенд), картинки бинарниками. Это навсегда снимает
  предупреждения LF/CRLF и защищает картинки от порчи. Одобрено.
- f5c08e0: docs — принято.
Итог: все четыре файла донора в финальном виде, посты чистые,
package.json здоров. Препятствий к перезаписи main нет.

## 031 · Команды (по одной строке, по порядку)
git fetch origin
git branch main-bak-2026-09-04 origin/main
git push origin main-bak-2026-09-04
git switch main
git reset --hard origin/main-qwen3-coder-plus-v01
git push --force-with-lease origin main
git switch main-qwen3-coder-plus-v01

Бак-ветку main-bak-2026-09-04 НЕ удалять: это ваш «начать сначала».
EdgeOne деплоит только main, бак безопасен.

## Приёмка вживую (после деплоя, по одной проверке)
1. Сайт открывается, шапка «Мемориал Корпечь, Крым», русский первый.
2. Открывается https://astro-blog.edgeone.dev/og/index.png
3. В коде страницы Ctrl+F «apiflash» — 0 совпадений.
4. Шаблонных постов нет (лента пустая или только ваши будущие тексты).
5. Вердикт по картинке — ваш.
Все пять зелёные → ТОЧКА СТОП донора. Дальше фаза «разместить ваши
тексты» (один текст за цикл) и, после неё, вилки по плану журнала.
Пришлите результат пяти проверок — закрою цикл и открою фазу текстов.
