<!-- ФАЙЛ: tasks/POPRVKA-node.md -->
# Поправка: чужая нода и тронутый docs

## Что случилось (коротко)
- Платформа дала Кодеру системную node v20.19.5. Файл `.nvmrc` сам по себе
  версию НЕ переключает — это делает nvm (или devcontainer, который
  платформа не применила). Значит установка (пункт 4) прошла под чужой нодой:
  нативные модули собраны под 20-ю — на стенде с 24.5.0 это «не полетит».
- Плюс в его копии помечен изменённым `docs/EXEC-LIST.md` — документы
  Кодеру трогать нельзя, вернуть к состоянию коммита.

## Правило (вошло в EXEC-LIST v4 и журнал)
Версия node в пространстве Кодера = версия стенда = `.nvmrc` = 24.5.0.
Сменил ноду — всегда переустанови `node_modules`.
Команды ДЛЯ СТЕНДА — только PowerShell; команды в своём пространстве —
в шелле своего пространства (там допустим bash).

## Ваши фразы (подавать по одной; отвал — стоп и мне)
П1: Ты изменил docs/EXEC-LIST.md — верни: git restore docs/EXEC-LIST.md
    Документы не трогать. Ответь выводом git status --short.
П2: Версия node неверна, стенд 24.5.0. Выполни:
    source $HOME/.nvm/nvm.sh && nvm install 24.5.0 && nvm alias default 24.5.0 && node -v
    Ответь выводом. Если nvm нет — NOT TESTED и покажи, чем ставится node.
П3: Выполни ТОЛЬКО: rm -rf node_modules — ответь OK.
П4: Выполни ТОЛЬКО: pnpm install --frozen-lockfile — ответь хвостом.
П5: Выполни ТОЛЬКО: node -v; pnpm -v; git status --short; git log -1 --oneline
    — ответь выводом (жду v24.5.0 и 10.33.0).
П6: Выполни ТОЛЬКО: pnpm run build — ответь хвостом.

После П6 принесите ответы — принимаю базовую линию, выдаю микро-фразы
правок конфига и Apiflash (пункты 10+ EXEC-LIST v4).