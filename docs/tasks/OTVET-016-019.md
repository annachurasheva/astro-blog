<!-- ФАЙЛ: tasks/OTVET-016-019.md -->
# 016–019 · Проверка d439c97 и ручные правки

## 016 · Что я увидел в ветке (сверка с планом)
- Head.astro: apiflashKey УДАЛЁН — коммит d439c97 принял. НО pageImage
  остался относительным — строка с new URL не доехала.
- `[...image].ts`: НЕ тронут (записи index нет) — даю полный файл (017).
- config.ts: СИНТАКСИЧЕСКАЯ ОШИБКА — после закомментированного folo осталась
  лишняя `},`. С таким файлом сборка падает всегда. Лечится заменой всего
  блока seo (018).
- config.ts содержательно: i18nTitle true (значит, ваш титул не работает —
  берётся из словаря); subtitle шаблонный; locale 'zh' (китайский первый!);
  комментарии enabled true с чужим waline-сервером; в футере чужой email.
- package.json: repository всё ещё чужой.
- final.patch больше не используем: переходим на ручные правки по списку.

## 017 · Полный файл src/pages/og/[...image].ts — отдельным блоком ниже
Замените ВЕСЬ файл на содержимое блока 017. Механика (logo/border/font/
fonts/bgGradient) не тронута — это часть заплатки под 100 баллов.

## 018 · Ручные правки «старая строка → новая строка»
config.ts:
1) `    i18nTitle: true, // true | false`
   → `    i18nTitle: false, // true | false`
2) `    subtitle: 'Revive the beauty of typography',`
   → `    subtitle: 'Памяти жертв политических репрессий',`
3) `    locale: 'zh', // de | en | es | fr | ja | ko | pl | pt | ru | zh | zh-tw`
   → `    locale: 'ru', // русский первый`
4) `    moreLocales: ['en', 'es', 'ja', 'ru', 'zh-tw'], // ['de', 'en', 'es', 'fr', 'ja', 'ko', 'pl', 'pt', 'ru', 'zh', 'zh-tw']`
   → `    moreLocales: ['en', 'es', 'ja', 'zh-tw'],`
5) `    enabled: true, // true | false` (секция COMMENT)
   → `    enabled: false, // true | false`
6) `        url: 'email@radishzz.cc',`
   → `        url: 'mailto:79787883649@ya.ru',`
7) Весь блок между маркерами SEO SETTINGS START и END заменить на:
   seo: {
     twitterID: '@Anna_Churasheva',
     verification: { google: '', bing: '', yandex: '', baidu: '' },
     googleAnalyticsID: '',
     umamiAnalyticsID: '',
   },
   (лишние `},` исчезают — скобки становятся сбалансированы)

Head.astro:
8) `const pageImage = `${base}/og/${postSlug ?? 'index'}.png``
   → `const pageImage = new URL(`${base}/og/${postSlug ?? 'index'}.png`, Astro.url.origin)`

package.json:
9) `"repository": "https://github.com/radishzzz/astro-theme-retypeset",`
   → `"repository": "https://github.com/annachurasheva/astro-blog",`

## 019 · После правок (по одному движению)
1. git ls-files src/content/posts — пришлите вывод (жду без .md;
   если есть — выполнить .\scripts\clean-posts.ps1 и жду «Осталось постов: 0»).
2. git add -A src package.json
   git commit -m "donor: manual fixes 016-019"
   git push
3. Пришлите хэш — я повторно прочту все четыре файла raw и сверю построчно.
4. Затем живой деплой и приёмка (сайт; /og/index.png; Ctrl+F «apiflash» = 0;
   шаблонных постов нет; вердикт по картинке ваш) → ТОЧКА СТОП донора.
```

```ts
// ФАЙЛ: src/pages/og/[...image].ts (полная замена, версия 017)
import type { CollectionEntry } from 'astro:content'
import { OGImageRoute } from 'astro-og-canvas'
import { getCollection } from 'astro:content'
import { themeConfig } from '@/config'
import { getPostDescription } from '@/utils/description'

// eslint-disable-next-line antfu/no-top-level-await
const posts = await getCollection('posts')

// Create slug-to-metadata lookup object for blog posts
type PageEntry = { title: string, description: string }

const pages: Record<string, PageEntry> = {
  // index page entry: превью главной страницы из локального генератора,
  // внешние сервисы (Apiflash и подобные) не используются
  index: {
    title: themeConfig.site.title,
    description: themeConfig.site.description,
  },
  // blog post entries
  ...Object.fromEntries(
    posts.map((post: CollectionEntry<'posts'>) => [
      post.id,
      {
        title: post.data.title,
        description: getPostDescription(post, 'og'),
      },
    ]),
  ),
}

// Configure Open Graph image generation route
// eslint-disable-next-line antfu/no-top-level-await
export const { getStaticPaths, GET } = await OGImageRoute({
  param: 'image',
  pages,
  getImageOptions: (_path, page) => ({
    title: page.title,
    description: page.description,
    logo: {
      path: './public/icons/og-logo.png', // Required local path and PNG format
      size: [250],
    },
    border: {
      color: [242, 241, 245],
      width: 20,
    },
    font: {
      title: {
        families: ['Noto Sans SC'],
        weight: 'Bold',
        color: [34, 33, 36],
        lineHeight: 1.5,
      },
      description: {
        families: ['Noto Sans SC'],
        color: [72, 71, 74],
        lineHeight: 1.5,
      },
    },
    fonts: [
      './public/fonts/NotoSansSC-Bold.otf',
      './public/fonts/NotoSansSC-Regular.otf',
    ],
    bgGradient: [[242, 241, 245]],
  }),
})
```
