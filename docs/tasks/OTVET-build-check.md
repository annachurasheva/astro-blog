<!-- ФАЙЛ: tasks/OTVET-build-check.md -->
# Ответ: сборку — на сторону EdgeOne, Кодеру — только проверка типов

## Согласен, ресурс не жжём
Известная проблема не лечится в пространстве Кодера: картинки шаблона
(sharp/apply-lqip) «ест» только деплой-сторона EdgeOne Pages.
Значит `pnpm run build` у Кодера убираем из списка вообще.
Вместо него — `pnpm exec astro check`: это проверка типов и синтаксиса,
картинки не трогает, ловит ошибки уровня того старого косяка с `zh-tw`.

## Новая раскладка приёмки
- Кодер: правки + `astro check` (PASS) + комплект zip.
- Полная сборка: происходит сама на EdgeOne после вашего переноса.
- Приёмка вживую (после деплоя): сайт открывается; `/og/index.png`
  открывается; в коде страницы Ctrl+F «apiflash» — ноль совпадений;
  вердикт по картинке — ваш.

## Ваша фраза вместо Н6
Н6: Выполни ТОЛЬКО: pnpm exec astro check — ответь хвостом.
    Полный build НЕ запускать: картинки шаблона ест только EdgeOne Pages,
    это известная проблема, ресурс не тратим.

Дальше по списку: пункты правок (10–14) как были, но вместо сборок —
тоже `astro check`. Всё это уже в EXEC-LIST v5 (блок 2).
После комплекта — ваш перенос и приёмка вживую по шагам выше.