<!-- ФАЙЛ: tasks/OTVET-091-093.md -->
# 091–093 · Понимание + инициализация

## 091 · Понимаю так (коротко)
- Дашборд context-vkru БРОШЕН: там «текучка» (задача→выполнение→сломалось),
  рядов под правку слишком много. Копия у вас в архиве — правильно.
- Журнал = простая ПОЯСНЯЛКА: «возьми ЭТО, положи сюда; удали ЭТО —
  станет хорошо». Без компонентной текучки.
- Новая головная папка: C:\astro-projects\; в ней три соседа:
  astro-blog (переписка/докы, скачается с GitHub), memorial-korpech-crimea
  (сделан, почищен), revision-of-astro-6-1-5 (пояснялка-дашборд внутри,
  «чуток больной» — переживём).
- Треки на GitHub под два новых репо уже созданы.
- Изоляция БОЛЬШЕ НЕ НУЖНА: на машине один аккаунт annachurasheva;
  код с обёрткой, прорвавшийся к админской записи, — урок; скрипты
  изоляции и ssh-обёртки отправляются в отставку.
- Git сносить НЕ стали (на компе есть невосполнимое) — верно.

## 092 · Отставки и замены
- git-isolated-sc.ps1 и C:\Tools\Scripts\ssh.bat — НЕ запускать никогда;
  в архив, как и context-vkru dashboard.
- Работаем простым git под одним аккаунтом: глобальная идентичность +
  ваш обычный SSH-ключ annachurasheva.
- Зеркало SC — отдельной командой позже, без обёрток.

## 093 · Инструкции (по одной за цикл)
Шаг 0 (один раз):
git config --global user.name
git config --global user.email
(если пусто: git config --global user.name "Anna Churasheva" и
git config --global user.email "anna.churasheva@gmail.com")
ssh -T git@github.com  → жду «Hi annachurasheva!»

Шаг 1 (astro-blog с перепиской):
git clone git@github.com:annachurasheva/astro-blog.git C:\astro-projects\astro-blog

Шаг 2 (memorial-korpech-crimea):
cd C:\astro-projects\memorial-korpech-crimea
git status --short   (глазами: нет node_modules/dist/.ssh/токенов;
если есть — сначала .gitignore с этими строками)
git init -b main
git add -A
git commit -m "init: memorial donor clean"
git remote add origin git@github.com:annachurasheva/memorial-korpech-crimea.git
git push -u origin main

Шаг 3 (revision-of-astro-6-1-5):
cd C:\astro-projects\revision-of-astro-6-1-5
(index.html пояснялки — в корень репо или в docs/; остальное как есть)
git status --short   (та же проверка глазами)
git init -b main
git add -A
git commit -m "init: revision explain-board"
git remote add origin git@github.com:annachurasheva/revision-of-astro-6-1-5.git
git push -u origin main
Затем GitHub → Settings → Pages → Deploy from branch → main → / (или /docs)
→ адрес https://annachurasheva.github.io/revision-of-astro-6-1-5/

После каждого шага — хэш/вывод мне; нюанс — стучитесь.


