```markdown
<!-- ФАЙЛ: tasks/OTVET-006.md -->
# 006 · Слева — не Кодер

- Ш2 сработало: Кодер ответил «OK» и шум в ответе не выводил.
- Список слева — это собственный интерфейс платформы (панель файлов
  диалога). Он не выключается и к ответам Кодера не относится.
  Уговор остаётся: на панель не смотрим; критерий — `git status --short`
  с четырьмя строками `M`, и он чистый.

## Следующие ходы (тексты фраз — в ответе 003)
1. Подать И1 (абсолютный og:image через new URL).
2. Подать И2 (вернуть eslint-disable над posts).
3. Подать И3 (astro check + полные диффы четырёх файлов).
После И3 принесите мне все четыре диффа — сверяю конфиг и package.json
(их я ещё не видел) и разрешаю перенос карандашом.
```