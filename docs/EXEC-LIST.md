<!-- ФАЙЛ: docs/EXEC-LIST.md -->
# EXEC-LIST v4 — список на исполнение для Кодера
Читать ПОСЛЕ `QWEN.md`. Журнал — `docs/JOURNAL.md`.

Лимит платформы: ~10 минут на ход. Один ход = одна команда или одна правка;
установка и сборка — всегда отдельные ходы; ответы короткие, правила
не пересказывать. Отвал = стоп и ждать команду проектировщика.
Команды ДЛЯ СТЕНДА — только PowerShell; команды в твоём пространстве —
в шелле твоего пространства.
Версия node у тебя = стенд = `.nvmrc` = 24.5.0; после смены node —
всегда переустановка `node_modules`.
Документы (`docs/**`, `QWEN.md`, `AGENTS.md`) НЕ изменять никогда.

## Пункт 1 (ход, выполнен). READ
## Пункт 2 (ход, выполнен). LIST
## Пункт 3 (ход, выполнен). corepack

## Пункт 4 (ход). Возврат документа
Только: `git restore docs/EXEC-LIST.md`
Ответить: `git status --short`.

## Пункт 5 (ход). Нода стендовой версии
Только: `source $HOME/.nvm/nvm.sh && nvm install 24.5.0 && nvm alias default 24.5.0 && node -v`
Если nvm отсутствует — статус NOT TESTED + показать, каким способом
в пространстве ставится node. Решения самому НЕ изобретать.

## Пункт 6 (ход). Чистая переустановка, часть 1
Только: `rm -rf node_modules`
Ответить: OK.

## Пункт 7 (ход). Чистая переустановка, часть 2
Только: `pnpm install --frozen-lockfile`
Ответить хвостом вывода.

## Пункт 8 (ход). Повторная фиксация стенда
Только: `node -v; pnpm -v; git status --short; git log -1 --oneline`
Жду: v24.5.0 и 10.33.0, в статусе без сюрпризов. Код НЕ трогать.

## Пункт 9 (ход). Базовая линия
Только: `pnpm run build` (БЕЗ правок кода).
Хвост вывода; статус PASS/FAIL. При FAIL — стоп, не чинить.

## Пункт 10 (ход). Конфиг: правка без сборки
В `src/config.ts` только перечисленное:
- убрать чужое: `umamiAnalyticsID`, `customUmamiAnalyticsJS` (`views.radishzz.cc`),
  `imageHostURL` (`image.radishzz.cc`);
- позывные донора: author `Anna Churasheva`;
  почты `anna.churasheva@gmail.com`, `79787883649@ya.ru`;
  название «Мемориал Корпечь, Крым»;
- `site.url` — оставить текущий тестовый домен EdgeOne;
- НЕ ТРОГАТЬ: комментарии, языки, остальное, механику файла.
Ответить кратким списком изменённых строк. Сборку НЕ запускать.

## Пункт 11 (ход). package.json
Только строка `repository` → `https://github.com/annachurasheva/astro-blog`.

## Пункт 12 (ход). Сборка после конфига
Только: `pnpm run build`. Хвост вывода.

## Пункт 13 (ход). Apiflash: Head.astro без сборки
Удалить обе ветки `api.apiflash.com` и ключ; `og:image` всегда
`${base}/og/${postSlug ?? 'index'}.png`. Механику (preload, отложенные
стили, partytown) НЕ трогать.

## Пункт 14 (ход). Apiflash: генератор без сборки
`src/pages/og/[...image].ts`: добавить запись `index`
(title/description из `themeConfig.site`).

## Пункт 15 (ход). Сборка и превью
Только: `pnpm run build`; проверить наличие `dist/og/index.png`.

## Пункт 16 (ход). Пакет передачи
Комплект ТОЛЬКО из: `src/config.ts`, `package.json`,
`src/layouts/Head.astro`, `src/pages/og/[...image].ts`.
Отдать кнопкой «Скачать комплект (.zip)». Ничего сверх.

## Дальше
Фаза «разместить тексты Заказчика» — задания проектировщика.
Очередь косяков из `docs/elimination-of-deficiencies.md` не трогать.
`update-theme` не запускать.