<!-- ФАЙЛ: docs/EXEC-LIST.md -->

# EXEC-LIST v3 — список на исполнение для Кодера
Читать ПОСЛЕ `QWEN.md`. Журнал — `docs/JOURNAL.md`.

Лимит платформы: ~10 минут на ход. Поэтому:
один ход = одна команда или одна правка; установка и сборка — всегда
отдельные ходы; ответы короткие (вывод/хэш/список), правила не пересказывать.
Отвал = остановиться, то же не повторять, ждать команду проектировщика.

## Пункт 1 (ход). Чтение правил
Прочитать `QWEN.md`. Ответить одной строкой: READ.

## Пункт 2 (ход). Чтение списка
Прочитать `docs/EXEC-LIST.md`. Ответить одной строкой: LIST.

## Пункт 3 (ход). Corepack
Только: `corepack enable && corepack prepare pnpm@10.33.0 --activate`
Ответить хвостом вывода.

## Пункт 4 (ход). Установка
Только: `pnpm install --frozen-lockfile`
Ответить хвостом вывода.

## Пункт 5 (ход). Фиксация стенда
Только: `node -v; pnpm -v; git status --short; git log -1 --oneline`
Ответить выводом. Код НЕ трогать.

## Пункт 6 (ход). Базовая линия
Только: `pnpm run build` (БЕЗ правок кода).
Ответить хвостом вывода; статус PASS/FAIL. При FAIL — стоп, не чинить.

## Пункт 7 (ход). Конфиг: правка без сборки
В `src/config.ts` только перечисленное:
- убрать чужое: `umamiAnalyticsID`, `customUmamiAnalyticsJS` (`views.radishzz.cc`),
  `imageHostURL` (`image.radishzz.cc`);
- позывные донора: author `Anna Churasheva`;
  почты `anna.churasheva@gmail.com`, `79787883649@ya.ru`;
  название «Мемориал Корпечь, Крым»;
- `site.url` — оставить текущий тестовый домен EdgeOne;
- НЕ ТРОГАТЬ: комментарии, языки, остальное, механику файлов.
Ответить: краткий список изменённых строк. Сборку в этом ходе НЕ запускать.

## Пункт 8 (ход). package.json
Только строка `repository` → `https://github.com/annachurasheva/astro-blog`.
Ответить: подтверждение + изменённая строка.

## Пункт 9 (ход). Сборка после конфига
Только: `pnpm run build`. Ответить хвостом вывода.

## Пункт 10 (ход). Apiflash: правка Head.astro без сборки
`src/layouts/Head.astro`: удалить обе ветки `api.apiflash.com` и ключ;
`og:image` всегда `${base}/og/${postSlug ?? 'index'}.png`.
Механику (preload, отложенные стили, partytown) НЕ трогать.
Ответить: краткий список изменённых строк.

## Пункт 11 (ход). Apiflash: правка генератора без сборки
`src/pages/og/[...image].ts`: добавить запись `index`
(title/description из `themeConfig.site`).
Ответить: краткий список изменённых строк.

## Пункт 12 (ход). Сборка и превью
Только: `pnpm run build`. Проверить, что создан `dist/og/index.png`.
Ответить хвостом вывода + факт наличия файла.

## Пункт 13 (ход). Пакет передачи
Собрать согласованный комплект ТОЛЬКО из четырёх файлов:
`src/config.ts`, `package.json`, `src/layouts/Head.astro`,
`src/pages/og/[...image].ts`.
Отдать кнопкой «Скачать комплект (.zip)». Ничего сверх комплекта.

## Дальше (после приёмки)
Фаза «разместить тексты Заказчика» — задания проектировщика, один текст
за цикл. Очередь косяков из `docs/elimination-of-deficiencies.md`
до конца фазы НЕ трогать. `update-theme` НЕ запускать никогда без задачи.